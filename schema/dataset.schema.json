{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://borzilleri.net/taplist-data/schema/dataset.schema.json",
  "title": "TapList dataset",
  "description": "A festival dataset: top-level metadata plus a list of beers. Loaded after the catalog using the URL the catalog provides. See docs/data-model.md §2 for the prose spec.",
  "type": "object",
  "required": ["id", "beers"],
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Stable dataset identifier. Used as the localStorage namespace key for user data, so it MUST remain stable across versions of the same dataset (e.g., daily updates of the WBF 2026 list all share id: \"wbf-2026\"). Slug-safe: lowercase letters, digits, and hyphens — no leading/trailing/double hyphens."
    },
    "festival": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable festival name. Shown in the UI header."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp of the last dataset update. Powers the 'updated X ago' indicator. If absent, the app falls back to the fetch time."
    },
    "mapUrl": {
      "type": "string",
      "format": "uri-reference",
      "minLength": 1,
      "description": "Optional venue/festival map image. An absolute URL or a root-relative path (re-anchored at the data base, like catalog entry URLs). Surfaced via the header map button in the app."
    },
    "beers": {
      "type": "array",
      "description": "The list of beers. Beers missing required fields (id, name, brewery) are dropped at load time with a warning.",
      "items": { "$ref": "#/$defs/beer" }
    }
  },
  "$defs": {
    "beer": {
      "type": "object",
      "required": ["id", "name", "brewery"],
      "additionalProperties": true,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable across dataset versions. User data is keyed off this. The dataset author commits to never re-using or re-purposing an id."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "The beer's display name."
        },
        "brewery": {
          "type": "string",
          "minLength": 1,
          "description": "The brewery's display name."
        },
        "abv": {
          "type": ["number", "null"],
          "minimum": 0,
          "maximum": 100,
          "description": "Percent alcohol by volume as a number (e.g., 5.2 — not \"5.2%\"). Optional; use null or omit when unknown. The runtime parser is lenient with stringly values like \"6.5%\" or \"5-7\" and treats them as missing, but they don't satisfy this schema."
        },
        "style": {
          "type": "string",
          "minLength": 1,
          "description": "Free-form style label (e.g., \"IPA\", \"Saison\", \"Imperial Stout\")."
        },
        "styleCategory": {
          "type": "string",
          "enum": [
            "IPA",
            "Pale Ale",
            "Lager & Pilsner",
            "Wheat",
            "Stout & Porter",
            "Sour & Gose",
            "Saison & Farmhouse",
            "Belgian & Strong",
            "Amber, Brown & Dark",
            "Blonde, Kölsch & Cream",
            "Cider",
            "Wine & Mead",
            "NA",
            "Other"
          ],
          "description": "Normalized style bucket for the list-view style filter, derived from the messy free-form `style`. Generated by scripts/categorize-styles.ts (see wbf-2026.NOTES.md). Optional — the runtime treats a missing or unknown value as \"Other\"."
        },
        "location": {
          "type": "string",
          "minLength": 1,
          "description": "Free-form location string (e.g., \"Booth 14\", \"North Tent, Booth 22\")."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Free-form long-form text. The brewery's own write-up, tasting notes, or backstory. No length cap."
        }
      }
    }
  }
}
